
#include<iostream>
using namespace std;
int n,num=0; 
int place[1001];
int judge(int k,int x)
{
    for(int i=1;i<=k-1;i++)
    if(place[i]==x)
    return 1;
    return 0;
}
void tryplace(int k)
{
    if(  ) // k>n or k>=n+1 
    return;
    for(int i=1;i<=n;i++)
    if(judge(k,i)==0)
    { 
        place[k]=i;
        try1(  );// k+1
    } 
}
int main()
{
    cin>>n;
    tryplace(1);
    cout<<num<<endl;
}

